# Core trading engine
httpx>=0.27
rich>=13.0
python-dotenv>=1.0

# MT5 local server (Windows only — requires MetaTrader5 terminal installed)
MetaTrader5>=5.0.45; sys_platform == "win32"
fastapi>=0.111
uvicorn>=0.29

# AI providers (install the one you use)
anthropic>=0.28         # Claude (pip install anthropic)
openai>=1.30            # OpenAI GPT (pip install openai)
google-generativeai>=0.7 # Gemini (pip install google-generativeai)

# Database logging (optional)
asyncpg>=0.29           # PostgreSQL async driver
